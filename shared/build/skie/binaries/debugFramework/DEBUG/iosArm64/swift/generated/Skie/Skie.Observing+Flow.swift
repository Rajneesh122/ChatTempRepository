// Generated by Touchlab SKIE 0.9.3

import Foundation
import SwiftUI

@available(iOS 15.0, macOS 12.0, tvOS 15.0, watchOS 8.0, *)
extension Shared.Observing {

    /**
     An instance observing a single flow. Look up the ``Observing`` view documentation for more information.
    
     - parameter flow1: The flow to observe.
     - parameter initialContent: View that's shown until the first element is emitted by the flow.
     - parameter content: View that's shown once a value is received from the flow and will be called for each new received value.
     */
    public init<Flow1 : Shared.SkieSwiftFlowProtocol>(
        _ flow1: Flow1,
        @SwiftUI.ViewBuilder initialContent: @escaping () -> InitialContent,
        @SwiftUI.ViewBuilder content: @escaping (Flow1.Element) -> Content
    ) where Flow1 : Shared.SkieSwiftFlowProtocol, Values == (Flow1.Element) {
        self.init(
            flows: [flow1],
            flowIds: [Swift.ObjectIdentifier(flow1.delegate)],
            initialContent: initialContent,
            content: content
        ) { values in
            guard let flowValue1: Flow1.Element = assertingSkieSwiftFlowValueUnwrap(value: values[0]) else { return nil }
            return flowValue1
        }
    }

    /**
     An instance observing 2 flows. Look up the ``Observing`` view documentation for more information.
    
     - parameter flow1: #1 flow to observe.
     - parameter flow2: #2 flow to observe.
     - parameter initialContent: View that's shown until the first element is emitted by the flow.
     - parameter content: View that's shown once a value is received from the flow and will be called for each new received value.
     */
    public init<Flow1 : Shared.SkieSwiftFlowProtocol, Flow2 : Shared.SkieSwiftFlowProtocol>(
        _ flow1: Flow1,
        _ flow2: Flow2,
        @SwiftUI.ViewBuilder initialContent: @escaping () -> InitialContent,
        @SwiftUI.ViewBuilder content: @escaping (Flow1.Element, Flow2.Element) -> Content
    ) where Flow1 : Shared.SkieSwiftFlowProtocol, Flow2 : Shared.SkieSwiftFlowProtocol,
            Values == (Flow1.Element, Flow2.Element) {
        self.init(
            flows: [flow1, flow2],
            flowIds: [Swift.ObjectIdentifier(flow1.delegate), Swift.ObjectIdentifier(flow2.delegate)],
            initialContent: initialContent,
            content: content
        ) { values in
            guard let flowValue1: Flow1.Element = assertingSkieSwiftFlowValueUnwrap(value: values[0]) else { return nil }
            guard let flowValue2: Flow2.Element = assertingSkieSwiftFlowValueUnwrap(value: values[1]) else { return nil }
            return (flowValue1, flowValue2)
        }
    }

    /**
     An instance observing 3 flows. Look up the ``Observing`` view documentation for more information.
    
     - parameter flow1: #1 flow to observe.
     - parameter flow2: #2 flow to observe.
     - parameter flow3: #3 flow to observe.
     - parameter initialContent: View that's shown until the first element is emitted by the flow.
     - parameter content: View that's shown once a value is received from the flow and will be called for each new received value.
     */
    public init<Flow1, Flow2, Flow3>(
        _ flow1: Flow1,
        _ flow2: Flow2,
        _ flow3: Flow3,
        @SwiftUI.ViewBuilder initialContent: @escaping () -> InitialContent,
        @SwiftUI.ViewBuilder content: @escaping (
            Flow1.Element,
            Flow2.Element,
            Flow3.Element
        ) -> Content
    ) where Flow1 : Shared.SkieSwiftFlowProtocol, Flow2 : Shared.SkieSwiftFlowProtocol,
            Flow3 : Shared.SkieSwiftFlowProtocol,
            Values == (Flow1.Element, Flow2.Element, Flow3.Element) {
        self.init(
            flows: [flow1, flow2, flow3],
            flowIds: [Swift.ObjectIdentifier(flow1.delegate), Swift.ObjectIdentifier(flow2.delegate), Swift.ObjectIdentifier(flow3.delegate)],
            initialContent: initialContent,
            content: content
        ) { values in
            guard let flowValue1: Flow1.Element = assertingSkieSwiftFlowValueUnwrap(value: values[0]) else { return nil }
            guard let flowValue2: Flow2.Element = assertingSkieSwiftFlowValueUnwrap(value: values[1]) else { return nil }
            guard let flowValue3: Flow3.Element = assertingSkieSwiftFlowValueUnwrap(value: values[2]) else { return nil }
            return (flowValue1, flowValue2, flowValue3)
        }
    }

    /**
     An instance observing 4 flows. Look up the ``Observing`` view documentation for more information.
    
     - parameter flow1: #1 flow to observe.
     - parameter flow2: #2 flow to observe.
     - parameter flow3: #3 flow to observe.
     - parameter flow4: #4 flow to observe.
     - parameter initialContent: View that's shown until the first element is emitted by the flow.
     - parameter content: View that's shown once a value is received from the flow and will be called for each new received value.
     */
    public init<Flow1, Flow2, Flow3, Flow4>(
        _ flow1: Flow1,
        _ flow2: Flow2,
        _ flow3: Flow3,
        _ flow4: Flow4,
        @SwiftUI.ViewBuilder initialContent: @escaping () -> InitialContent,
        @SwiftUI.ViewBuilder content: @escaping (
            Flow1.Element,
            Flow2.Element,
            Flow3.Element,
            Flow4.Element
        ) -> Content
    ) where Flow1 : Shared.SkieSwiftFlowProtocol, Flow2 : Shared.SkieSwiftFlowProtocol,
            Flow3 : Shared.SkieSwiftFlowProtocol, Flow4 : Shared.SkieSwiftFlowProtocol,
            Values == (Flow1.Element, Flow2.Element, Flow3.Element, Flow4.Element) {
        self.init(
            flows: [flow1, flow2, flow3, flow4],
            flowIds: [Swift.ObjectIdentifier(flow1.delegate), Swift.ObjectIdentifier(flow2.delegate), Swift.ObjectIdentifier(flow3.delegate), Swift.ObjectIdentifier(flow4.delegate)],
            initialContent: initialContent,
            content: content
        ) { values in
            guard let flowValue1: Flow1.Element = assertingSkieSwiftFlowValueUnwrap(value: values[0]) else { return nil }
            guard let flowValue2: Flow2.Element = assertingSkieSwiftFlowValueUnwrap(value: values[1]) else { return nil }
            guard let flowValue3: Flow3.Element = assertingSkieSwiftFlowValueUnwrap(value: values[2]) else { return nil }
            guard let flowValue4: Flow4.Element = assertingSkieSwiftFlowValueUnwrap(value: values[3]) else { return nil }
            return (flowValue1, flowValue2, flowValue3, flowValue4)
        }
    }

    /**
     An instance observing 5 flows. Look up the ``Observing`` view documentation for more information.
    
     - parameter flow1: #1 flow to observe.
     - parameter flow2: #2 flow to observe.
     - parameter flow3: #3 flow to observe.
     - parameter flow4: #4 flow to observe.
     - parameter flow5: #5 flow to observe.
     - parameter initialContent: View that's shown until the first element is emitted by the flow.
     - parameter content: View that's shown once a value is received from the flow and will be called for each new received value.
     */
    public init<Flow1, Flow2, Flow3, Flow4, Flow5>(
        _ flow1: Flow1,
        _ flow2: Flow2,
        _ flow3: Flow3,
        _ flow4: Flow4,
        _ flow5: Flow5,
        @SwiftUI.ViewBuilder initialContent: @escaping () -> InitialContent,
        @SwiftUI.ViewBuilder content: @escaping (
            Flow1.Element,
            Flow2.Element,
            Flow3.Element,
            Flow4.Element,
            Flow5.Element
        ) -> Content
    ) where Flow1 : Shared.SkieSwiftFlowProtocol, Flow2 : Shared.SkieSwiftFlowProtocol,
            Flow3 : Shared.SkieSwiftFlowProtocol, Flow4 : Shared.SkieSwiftFlowProtocol,
            Flow5 : Shared.SkieSwiftFlowProtocol,
            Values == (Flow1.Element, Flow2.Element, Flow3.Element, Flow4.Element, Flow5.Element) {
        self.init(
            flows: [flow1, flow2, flow3, flow4, flow5],
            flowIds: [Swift.ObjectIdentifier(flow1.delegate), Swift.ObjectIdentifier(flow2.delegate), Swift.ObjectIdentifier(flow3.delegate), Swift.ObjectIdentifier(flow4.delegate), Swift.ObjectIdentifier(flow5.delegate)],
            initialContent: initialContent,
            content: content
        ) { values in
            guard let flowValue1: Flow1.Element = assertingSkieSwiftFlowValueUnwrap(value: values[0]) else { return nil }
            guard let flowValue2: Flow2.Element = assertingSkieSwiftFlowValueUnwrap(value: values[1]) else { return nil }
            guard let flowValue3: Flow3.Element = assertingSkieSwiftFlowValueUnwrap(value: values[2]) else { return nil }
            guard let flowValue4: Flow4.Element = assertingSkieSwiftFlowValueUnwrap(value: values[3]) else { return nil }
            guard let flowValue5: Flow5.Element = assertingSkieSwiftFlowValueUnwrap(value: values[4]) else { return nil }
            return (flowValue1, flowValue2, flowValue3, flowValue4, flowValue5)
        }
    }

}
